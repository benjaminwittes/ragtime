# Cloudflare Wrangler local build / dev cache
.wrangler/
worker/.wrangler/

# Node dev toolchain (installed globally / not committed)
node_modules/

# Stray root-level build output (legit app build is app/dist, ignored in app/.gitignore)
/dist/

# Local Claude Code + sandbox artifacts
.claude/
.sandbox-partial-pull/
.DS_Store

.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Local load-test harness — operational, not source
loadtest/
