import './App.css'

function App() {
  return (
    <main className="scaffold-welcome">
      <h1>RAGtime</h1>
      <p className="tagline">React rebuild — scaffold (PR 1 of the foundation work).</p>
      <p>
        This is an empty Vite + React + TypeScript shell. The production site
        is still served from the single-file <code>index.html</code> at the
        repo root via GitHub Pages; this React app will eventually replace it
        once the foundation work and per-corpus spokes are ported.
      </p>
      <p>
        See the per-surface query-design briefs (1–7) in iCloud and{' '}
        <code>CLAUDE.md</code> for the larger plan.
      </p>
    </main>
  )
}

export default App
